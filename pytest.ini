[pytest]
# Tell pytest where to find tests
testpaths = tests

# Show verbose output with test names
addopts = -v --tb=short

# Warn about any deprecations
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning
